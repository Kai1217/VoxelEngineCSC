using System;
using OpenTK;
using OpenTK.Graphics;
using OpenTK.Graphics.OpenGL;
using OpenTK.Graphics.OpenGL4;


namespace VoxelEngineCSC
{
    public class Vectors
    {
        protected void UpdateVectors()
        {
            /* 
           foward.x = Math.Sin(Convert.ToFloat(angle.x)); 
           foward.y = 0;
           foward.z = Math.Cos(Convert.ToFloat(angle.x));

           right.x = -Math.Cos(Convert.ToFloat(angle.x));
           right.y = 0;
           right.z = -Math.Sin(Convert.ToFloat(angle.x));

           lookat.x = Math.Sin(Convert.ToFloat(angle.x) * Math.Cos(Convert.ToFloat(angle.y)));
           lookat.y = Math.Sin(Convert.ToFloat(angle.y));
           lookat.z = Math.Cos(Convert.ToFloat(angle.x) * Math.Cos(Convert.ToFloat(angle.y)));

           // TODO: up variable    
           */    
        }
    }
}